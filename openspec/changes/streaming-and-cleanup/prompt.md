# Prompt de Retomada

Continuar `openspec/changes/streaming-and-cleanup/`.

Objetivo: fechar streaming visual customer com estado `in_progress`, remover stat de marketing da home customer, tornar Brand clicavel para `/`, e limpar leaks residuais (`OP-1002`, ID tecnico/fallback fora de debug, HUD AO VIVO apos status terminal).

Invariantes:

- Backend intocado.
- Operator preserva telemetria/throughput.
- Densidade aquamarine >= 2.5%.
- `prefers-reduced-motion` desliga pulse/cursor animados.
- Validar screenshot mid-simulation com PROCESSANDO visivel.
